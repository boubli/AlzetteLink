{
  "name": "alzettelink-bridge",
  "version": "1.0.0",
  "description": "MQTT to InfluxDB bridge for AlzetteLink",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test tests/bridge.test.js",
    "test:watch": "node --test --watch tests/"
  },
  "dependencies": {
    "mqtt": "^5.0.0",
    "@influxdata/influxdb-client": "^1.33.0",
    "dotenv": "^16.0.0"
  }
}
